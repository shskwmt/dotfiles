version: '3'

tasks:
  default:
    cmds:
      - task: brewfile
      - task: bundle

  clean:
    desc: Clean homebrew
    cmds:
      - task: brewfile:clean

  brewfile:
    desc: Create symlink
    cmds:
      - |
        if [ -e "~/.brewfile" -a ! -L "~/.brewfile" ]; then
          mv "~/.brewfile" "~/.brewfile.bak"
        fi
        if [ ! -e "~/.brewfile" ]; then
          ln -s "{{ .DOTFILE_DIR }}/homebrew/brewfile" "$HOME/.brewfile"
        fi
    sources:
      - ./homebrew/brewfile

  brewfile:clean:
    desc: Clean brewfile
    cmds:
      - rm "~/.brewfile"

  bundle:
    desc: Brew bundle
    cmds:
      - cmd: |
          if [ -d "~/.linuxbrew" ]; then
            eval "$(~/.linuxbrew/bin/brew shellenv)"
          fi
        platforms: [linux]
      - cmd: |
          if [ -d "/home/linuxbrew/.linuxbrew" ]; then
            eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
          fi
        platforms: [linux]
      - brew bundle --file "./homebrew/brewfile"
